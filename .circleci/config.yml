version: 2

jobs:
  build:
    docker:
      - image: maxmcd/wasabi:circleci
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: print done
          command: |
            echo done
